#![allow(unused)]

mod collection;
mod debug_display;
mod r#enum;
mod exception;
mod expression;
mod r#format;
mod lifetime;
mod mutable;
mod ownership;
mod reference;
mod scalar;
mod structure;
mod template;
mod test;

fn main() {
    // collection::collection();
    // debug_display::debug_display();
    // r#enum::r#enum();
    // exception::exception();
    // expression::expression();
    // r#format::r#format();
    // lifetime::lifetime();
    // mutable::mutable();
    // ownership::ownership();
    // reference::reference();
    // scalar::scalar();
    // structure::structure();
    // template::template();
}
