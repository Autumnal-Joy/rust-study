mod collection;
mod debug_display;
mod r#enum;
mod expression;
mod r#format;
mod mutable;
mod ownership;
mod reference;
mod scalar;
mod structure;

fn main() {
    // collection::collection();
    // debug_display::debug_display();
    // r#enum::r#enum();
    // expression::expression();
    // r#format::r#format();
    // mutable::mutable();
    // ownership::ownership();
    // reference::reference();
    // scalar::scalar();
    // structure::structure();
}
